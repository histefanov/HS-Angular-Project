import { EqualValueDirective } from './equal-value.directive';

describe('EqualValueDirective', () => {
  it('should create an instance', () => {
    const directive = new EqualValueDirective();
    expect(directive).toBeTruthy();
  });
});
